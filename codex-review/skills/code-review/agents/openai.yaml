interface:
  display_name: "Code Review"
  short_description: "Review code with contextual quality rules"
  default_prompt: "Use $code-review to review the provided code changes against all applicable rules."
